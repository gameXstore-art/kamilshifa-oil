// Shop Now scroll document.getElementById("shopNowBtn").addEventListener("click", () => { document.getElementById("products").scrollIntoView({ behavior: "smooth" }); }); // Add to Cart Animation document.querySelectorAll(".cart-btn").forEach((btn) => { btn.addEventListener("click", () => { btn.textContent = "✓ Added!"; btn.style.backgroundColor = "#4caf50"; setTimeout(() => { btn.textContent = "Add to Cart"; btn.style.backgroundColor = "#00b4d8"; }, 2000); }); }); // Contact form document.getElementById("contactForm").addEventListener("submit", (e) => { e.preventDefault(); alert("Thanks for contacting ReliefOil! We’ll reply soon."); e.target.reset(); });